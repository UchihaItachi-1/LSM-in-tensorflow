import numpy as np
import tensorflow as tf
print(tf.version)

def init_reservoir():
    return

def run_input(input_spike):
    reservoir_spike_pattern = 0
    return reservoir_spike_pattern
#lol
#added comment

if __name__ == '__main__':
    # runtime_para, res_para, Gin, Gres, neuron_model, synapse_model
    input_spike = np.array(list(map(int, input().split())))
    run_input(input_spike)



